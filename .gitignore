__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# AWS SSH private key — never commit this
*.pem

# Decision log and local shared-pool files are regenerated by every
# demo/test run — don't ship stale state.
policy/logs/
shared_store/pool*.jsonl

# Raw dataset files are large and not ours to redistribute; document the
# download step in data/README.md instead.
data/raw/
data/*.csv
