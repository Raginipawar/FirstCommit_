# Person 2 — Policy & Abstraction
cedarpy>=4.12.0

# Person 1 — Detection
scikit-learn>=1.9.0
pandas>=3.0.0
numpy>=2.5.0

# Person 3 — Orchestration & Memory
# opensearch-py powers OpenSearchPatternStore, but LocalPatternStore (no
# extra dependency) is what actually runs today -- see shared_store/README.md.
opensearch-py>=3.0.0

# Testing (all components)
pytest>=8.0.0

# Live backend for the site's "Live Run" section (server.py) — wraps the
# real pipeline above in two HTTP endpoints, no mocking, see server.py
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Real Strands-wrapped orchestration (orchestration/strands_agent.py,
# scripts/run_strands_swarm.py) — backed by a local Ollama model, since this
# environment has no cloud model credentials. Also needs, one time:
#   winget install --id Ollama.Ollama -e
#   ollama pull qwen2.5:1.5b
strands-agents>=1.0.0
strands-agents-tools>=1.0.0
ollama>=0.4.0
